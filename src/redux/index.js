export { selectContacts } from './contacts/selectors';
export { addContact, deleteContact } from './contacts/slice';

export { selectFilter } from './filter/selectors';
export { setFilter, filterReducer } from './filter/slice';

export { persistedContactsReducer } from './contacts/slice';
